#include <bits/stdc++.h> 
int atoi(string str) {
    // Write your code here.
    int res=0;
    for(int i=0;i<str.length();i++){
        if((str[i]-'0')>=0 && (str[i]-'0')<=9){
            res=res*10+(str[i]-'0');
        }
    }
    if(str[0]=='-'){
        res=-res;
    }
    return res;
}
